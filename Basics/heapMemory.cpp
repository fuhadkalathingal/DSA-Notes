#include <bits/stdc++.h>
using namespace std;

int main() {
    int **arr = new int *[10];
    for(int i = 0; i < 10; i++) {
        int siz;
        cin >> siz;
        arr[i] = new int[siz];
    }

    return 0;
}