#include <bits/stdc++.h>
using namespace std;

int main() {

    string str;
    str.size(); //size of string
    reverse(str.begin(), str.end()); //reverse the string
    str.clear(); // empty the string
    str.empty(); // to check wheather a string is empty or not
    str.front(); //first character of string
    str.back(); //last character of string
    str.append("Hello"); //adding hello to string
    str.substr(0,1); //print the specified index
    

    return 0;
}