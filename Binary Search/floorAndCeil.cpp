#include <bits/stdc++.h>
using namespace std;

int main() {
    //floor means - longest n.o in array <= x
    //ceil - smallest n.o in array >= x;

    int n = 5;
    vector<int> arr = {10, 20, 30, 40, 50};
    int x = 25;

    return 0;
}